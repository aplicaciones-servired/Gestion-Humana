---
import SiteLayout from "../layouts/LayautHome.astro";
import Welcome from "../components/Welcome";

// Obtener el rol del usuario desde locals (establecido por el middleware)
const userRole = Astro.locals.userRole;
---

<SiteLayout>
  <Welcome client:load userRole={userRole} />
</SiteLayout>
